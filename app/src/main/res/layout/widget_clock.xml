<?xml version="1.0" encoding="utf-8"?>
<!-- Clock widget layout — large time + date + greeting, Infinix/Oppo style -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingVertical="12dp">

    <!-- Greeting -->
    <TextView
        android:id="@+id/tv_clock_greeting"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:textColor="#CCFFFFFF"
        android:letterSpacing="0.05"
        android:text="Selamat Pagi ☀️" />

    <!-- Time — large, bold -->
    <TextView
        android:id="@+id/tv_clock_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="68sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:letterSpacing="-0.02"
        android:shadowColor="#55000000"
        android:shadowDx="0"
        android:shadowDy="2"
        android:shadowRadius="8"
        android:text="12:00 AM" />

    <!-- Date -->
    <TextView
        android:id="@+id/tv_clock_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="-4dp"
        android:textSize="15sp"
        android:textColor="#DDFFFFFF"
        android:letterSpacing="0.02"
        android:text="Monday, 01 January 2025" />

</LinearLayout>
