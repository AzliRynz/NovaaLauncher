<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.EXPAND_STATUS_BAR" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NovaaLauncher">

        <!-- ===== MAIN HOME ACTIVITY ===== -->
        <activity
            android:name=".activities.HomeActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:theme="@style/Theme.NovaaLauncher.Launcher">
            <intent-filter android:priority="1">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.MONKEY" />
            </intent-filter>
        </activity>

        <!-- ===== APP DRAWER ACTIVITY ===== -->
        <activity
            android:name=".activities.AppDrawerActivity"
            android:exported="false"
            android:theme="@style/Theme.NovaaLauncher.Translucent"
            android:windowSoftInputMode="adjustResize" />

        <!-- ===== SETTINGS ACTIVITY ===== -->
        <activity
            android:name=".activities.LauncherSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.NovaaLauncher"
            android:label="@string/launcher_settings" />

        <!-- ===== WALLPAPER PICKER ACTIVITY ===== -->
        <activity
            android:name=".activities.WallpaperPickerActivity"
            android:exported="false"
            android:theme="@style/Theme.NovaaLauncher" />

        <!-- ===== WIDGET PICKER ACTIVITY ===== -->
        <activity
            android:name=".activities.WidgetPickerActivity"
            android:exported="false"
            android:theme="@style/Theme.NovaaLauncher" />

        <!-- ===== FOLDER POPUP ===== -->
        <activity
            android:name=".activities.FolderActivity"
            android:exported="false"
            android:theme="@style/Theme.NovaaLauncher.Translucent"
            android:windowSoftInputMode="adjustResize" />

        <!-- ===== ICON PACK PICKER ===== -->
        <activity
            android:name=".activities.IconPackPickerActivity"
            android:exported="false"
            android:theme="@style/Theme.NovaaLauncher" />

        <!-- ===== BOOT RECEIVER ===== -->
        <receiver
            android:name=".services.BootReceiver"
            android:exported="true">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- ===== PACKAGE CHANGE RECEIVER ===== -->
        <receiver
            android:name=".services.PackageChangeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <action android:name="android.intent.action.PACKAGE_CHANGED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
